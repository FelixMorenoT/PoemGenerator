class Rule:
    def __init__(self, rule_id, definitions, references, keyword):
        self.rule_id = rule_id
        self.rule_definition = definitions
        self.rule_references = references
        self.rule_keyword = keyword
